# R11-M1–M22 verification (2026-05-22)

| Task | Status | Notes |
|------|--------|-------|
| R11-M1 | ✅ | Session cached via React Query in `require-auth` |
| R11-M2 | ✅ | Editor `validateSearch` uses shared Zod enum |
| R11-M3 | ✅ | Invite/shared pages use bounded `staleTime` + refetch on focus |
| R11-M4 | ✅ | RpcError code-based toasts (partial; API schemas shared) |
| R11-M5 | ✅ | `formContent` query keys scoped by draft/preview |
| R11-M6 | ✅ | SSE `consecutiveErrors` hoisted outside `connect()` (this PR) |
| R11-M7 | ✅ | `response-export` deferred `revokeObjectURL` (this PR) |
| R11-M8 | ⬜ | `useGoogleOAuth` extraction deferred (H10 done) |
| R11-M9 | ✅ | Discord guild permissions Zod + safe BigInt (R10-H4 overlap) |
| R11-M10 | ✅ this PR | `findGuildMemberByUsername` paginates Discord member search |
| R11-M11 | ✅ this PR | `withRedisLock` accepts worker shutdown AbortSignal |
| R11-M12 | ✅ | Sheets header collision suffix in `sheets-sync` |
| R11-M13 | ✅ | OAuth scope fallback uses `currentToken.scopes` |
| R11-M14 | ✅ | Cancel returns 409 when job active (R10-H1 chain) |
| R11-M15 | ✅ | Same as R11-C2-c (`jobId IS NULL`) |
| R11-M16 | ✅ | `RedisChannelFormIdSchema` max 128 aligned with DB |
| R11-M17 | ✅ this PR | `system-settings.ts` Zod keys + API read/write validation |
| R11-M18 | ✅ this PR | Drop global `respondentUuid` unique; keep `formId`+`respondentUuid` index |
| R11-M19 | ✅ this PR | `db:generate` turbo inputs/outputs + cache |
| R11-M20 | ✅ this PR | Root `tsconfig.json` `@nexus-form/*` path aliases |
| R11-M21 | ✅ this PR | MySQL/Redis `healthcheck` + `service_healthy` depends_on |
| R11-M22 | ✅ this PR | lefthook `db:schema:check-staged`; CI migrate in #294 |
