# R8-T1 verification (2026-05-22)

React Doctor regression after R8-H1–M14 / L1–L2 and R8-M12 dead-code slices 1–3.

## React Doctor (`react-doctor@0.0.47`)

Command (baseline per tasks.md):

```bash
cd apps/web && npx react-doctor@0.0.47 . --full --offline --json --fail-on none
```

| Metric | Result |
|--------|--------|
| Score | 90 / 100 (Great) |
| Errors | **0** |
| Warnings | 197 |
| Affected files | 108 / 379 |

Prior correctness errors (`react-doctor/no-nested-component-definition` from R8-H1) remain resolved.

Note: `react-doctor@0.2.x` reports 28 `only-export-components` fast-refresh errors in shadcn/ui and Plate nodes; the project baseline uses v0.0.47 for regression gating.

### Follow-up (deferred, not blocking R8-T1)

The 28 v0.2.x errors are fast-refresh lint on shadcn/ui barrels (`buttonVariants`, `badgeVariants`, etc.) and Plate form-question nodes with module-local subcomponents. They are the same class of intentional retains as R8-M12 knip false positives — not runtime correctness bugs. Revisit when adopting `react-doctor@0.2.x` as the project baseline (split variant exports / hoist local nodes, or upstream shadcn pattern change).

## Repo verification

- [x] `pnpm lint:fix`
- [x] `pnpm type-check`
- [x] `pnpm test --silent`

## R8-M12 knip residual (expected false positives)

After slice 3, `knip` still flags ~33 `components/ui/*` shadcn barrels, `public/env-config.js`, and `hooks/use-mobile.ts` (only imported by unused sidebar). These are documented intentional retains per `r8-m12-slice1.md`–`slice3.md`.
