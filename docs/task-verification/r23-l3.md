# R23-L3 verification (2026-06-01)

## 実施内容

- `docs/operations.md` に「R23-L3 安全 QA 環境」を追加し、前提、禁止事項、非秘密 env、対象リソース準備、機能別手順、終了後 cleanup を整理した。
- `e2e/README.md` に QA fixture 方針を追記し、手動 E2E 前提と placeholder 運用を明記した。
- 本変更は docs-only であり、runtime code、schema、package、CI、`.env.example` は変更していない。

## Full validation

変更範囲は docs-only で runtime code、schema、package、CI、env 値を変更していないため full test waiver 可能な範囲だったが、PR 前の確認として `pnpm lint:fix`、`pnpm type-check`、`pnpm test -- --silent` も実行し、すべて pass した。

## 検証項目

| 項目 | 結果 | Notes |
|------|------|-------|
| 専用テストアカウント準備手順 | ✅ | `qa-owner@example.test`, `qa-invitee@example.test` の placeholder を使用 |
| テスト Google Sheet 準備手順 | ✅ | `<QA_GOOGLE_SHEET_ID>` placeholder を使用 |
| テストメール受信先準備手順 | ✅ | `qa-mailbox@example.test` の placeholder を使用 |
| 短時間スケジュール用フォーム準備手順 | ✅ | `R23-L3 QA Schedule Form` を例示 |
| 共有リンクの安全確認手順 | ✅ | token を `<SHARE_TOKEN>` に置換する運用を明記 |
| 招待の安全確認手順 | ✅ | メール sink/mailbox と token 非記録を明記 |
| 権限変更/削除の安全確認手順 | ✅ | QA owner / QA invitee 間だけで変更、削除、失効後アクセス不可を確認 |
| Sheets 同期の安全確認手順 | ✅ | QA Sheet 限定と本番 Sheet 禁止を明記 |
| スケジュール保存の安全確認手順 | ✅ | QA フォーム限定の保存/境界確認を明記 |
| 公開/非公開/スナップショット切替の安全確認手順 | ✅ | QA フォームの表示状態と QA 用スナップショットだけを確認 |
| 本番データ、実秘密情報、外部サービス実接続の禁止 | ✅ | 禁止事項として明記 |
| hCaptcha 対象外 | ✅ | hCaptcha 関連を変更しないことを明記 |

## 秘密情報なし確認

- 実秘密情報、実認証情報、実メールアドレス、実 Google Sheet ID は追加していない。
- 使用したメールアドレスは `example.test` の placeholder のみ。
- 使用した ID は `<QA_GOOGLE_SHEET_ID>`、`<QA_SHORT_SCHEDULE_FORM_ID>`、`<SHARE_TOKEN>` の placeholder のみ。
- hCaptcha 関連 env や `.env.example` は変更していない。

## コマンド結果

| Command | Result | Notes |
|---------|--------|-------|
| `rtk git diff --check` | pass | whitespace error なし |
| manual diff review | pass | 差分と新規ファイルに秘密情報らしい値がなく、hCaptcha 関連は対象外明記のみで設定変更なし |
| `rtk pnpm lint:fix` | pass | 全 workspace 成功、No fixes applied |
| `rtk pnpm type-check` | pass | Turbo type-check 成功 |
| `rtk pnpm test -- --silent` | pass | Turbo test 成功 |
