# R11-C1–C5 verification (2026-05-22)

| Task | Status | Implementation | Tests |
|------|--------|----------------|-------|
| R11-C1 | ✅ | `forms-integrations.ts` validates `job.data.formId` + `integrationId` before returning job state | `forms-integrations-google-sheets-sync-auth.test.ts` |
| R11-C2-a | ✅ | `forms-public.ts` tx inserts `PENDING` validation rows with response | `forms-public-validation-outbox.test.ts` (`R11-C2-a`) |
| R11-C2-b | ✅ | `validation-outbox-sweeper.ts` + API startup hook | `validation-outbox-sweeper.test.ts` |
| R11-C2-c | ✅ | jobId UPDATE uses `isNull(jobId)` guard | `forms-public-validation-outbox.test.ts` |
| R11-C3 | ✅ | Dead `forms-permissions` accept route removed; canonical `/invites/:token/accept` | `forms-invitations-accept-auth.test.ts` (`R11-C3`) |
| R11-C4 | ✅ | Root `.dockerignore` excludes `.env*`, `node_modules`, secrets | Repo invariant (see `r11-t1.md`) |
| R11-C5 | ✅ | `use-autosave.ts` lazy `getRespondentUuid()` sync init | Hook uses `resolveRespondentUuid` on first access |
