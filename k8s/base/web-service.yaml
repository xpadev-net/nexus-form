apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app: nexus-form
    component: web
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: nexus-form
    component: web
