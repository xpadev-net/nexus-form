apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app: nexus-form
    component: api
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: nexus-form
    component: api

